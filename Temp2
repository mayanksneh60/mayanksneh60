IMAGE_TOKEN = "<image>"

if IMAGE_TOKEN not in tokenizer.get_vocab():
    tokenizer.add_special_tokens({"additional_special_tokens": [IMAGE_TOKEN]})
    model.resize_token_embeddings(len(tokenizer))


image_tokens = " ".join(["<image>"] * IMAGE_TOKEN_SIZE)


