with torch.no_grad():
    dummy = torch.zeros(1, 3, 448, 448).to(
        DEVICE,
        dtype=next(model.parameters()).dtype
    )
    vit = model.extract_feature(dummy)
    IMAGE_TOKEN_LEN = vit.shape[1]

print("Image token length:", IMAGE_TOKEN_LEN)
