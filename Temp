Triple<book_flight, Books a flight ticket, {"from": "string", "to": "string", "date": "string", "passenger_count": "int"}>
Triple<check_weather, Retrieves weather information for a location, {"location": "string", "date": "string"}>
Triple<set_alarm, Sets an alarm for a given time, {"time": "string", "label": "string"}>
Triple<play_music, Plays music based on genre or artist, {"genre": "string", "artist": "string"}>
Triple<send_message, Sends a message to a contact, {"contact_name": "string", "message": "string"}>
Triple<navigate_to_location, Opens maps and navigates to a location, {"destination": "string"}>
Triple<book_movie_ticket, Books a movie ticket, {"movie_name": "string", "theatre": "string", "date": "string", "time": "string", "seat_count": "int"}>
Triple<order_food, Places a food order from a restaurant, {"restaurant": "string", "items": ["string"], "delivery_address": "string"}>
Triple<create_calendar_event, Adds an event to the calendar, {"title": "string", "date": "string", "time": "string", "location": "string"}>
Triple<translate_text, Translates text from one language to another, {"text": "string", "source_language": "string", "target_language": "string"}>
