import ijson

filename = "coco.json"

with open(filename, "rb") as f:
    annotations = ijson.items(f, "annotations.item")
    for ann in annotations:
        print(ann)
        break   # remove break to load all
